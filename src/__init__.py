"""Source package for the Automated Essay Scoring 2.0 baseline.

Present so ``import src.features`` resolves regardless of the current working
directory (as long as the repo root is on ``sys.path``).
"""
