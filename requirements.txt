# CPU-only baseline dependencies for Automated Essay Scoring 2.0.
# Pinned to the versions the pipeline was developed and verified against.
# Install: python -m venv .venv && .venv/bin/pip install -r requirements.txt
numpy==2.5.1
pandas==3.0.3
scipy==1.18.0
scikit-learn==1.9.0

# Optional — only needed for scripts/train_gbdt.py (the LightGBM model).
# The rest of the pipeline (Ridge / LogReg / SVR / blend) runs without it, and
# train_gbdt.py self-skips if it is missing. On macOS LightGBM also needs the
# OpenMP runtime: `brew install libomp`.
lightgbm==4.6.0

# Optional — only needed for scripts/hf_embeddings.py (frozen MiniLM sentence
# embeddings, CPU inference only, no fine-tuning). hf_embeddings.py and the
# HF-dependent scripts (train_hf_ridge.py, train_gbdt_hf.py) self-skip if these
# are absent. First run downloads sentence-transformers/all-MiniLM-L6-v2 into
# the HF cache; on an offline box, pre-populate that cache (the scripts set
# HF_HUB_OFFLINE=1 and read it directly).
torch==2.13.0
transformers==5.13.1


